export default function Footer() {
    return (
      <footer className="border-t border-white/5 py-10 text-center text-sm text-mute">
        <p>© {new Date().getFullYear()} OSMOS — Crafted with clarity, not noise.</p>
      </footer>
    );
  }
  